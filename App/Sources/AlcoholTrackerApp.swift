import SwiftUI

/// Application entry point. The scaffolding milestone ships only a placeholder
/// root view; real navigation and tracking features arrive in later milestones.
@main
struct AlcoholTrackerApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
